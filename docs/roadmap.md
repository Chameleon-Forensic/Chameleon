# Yapılacaklar / fikirler

## Yapıldı

- SSH motoru (`ssh_engine`) ve RAM imaj motoru (`ram_engine`) tek launcher'dan seçilebiliyor.
- Launcher ve SSH motoru tam CustomTkinter (açık/koyu tema, tutarlı renk paleti).
- Güvenlik: `disk_path` artık `shlex.quote` ile korunuyor (komut enjeksiyonu kapatıldı, `image_acquirer.py` + `write_block_helper.py`).
- Çift disk alanı sorunu çözüldü: `concatenate_blocks(..., cleanup=...)` — Live modda parçalar korunuyor (resume için), Offline modda birleştirme sonrası otomatik siliniyor. Hem GUI hem CLI güncellendi, test edildi.
- RAM motoru artık kendi CTk arayüzümüzle (`engines/ram_engine/ram_gui.py`) çalışıyor — `RamImagerGUI.exe` (vendor'in WinForms programı) hiç açılmıyor, `RamImagerCLI.exe` doğrudan çağrılıyor. Launcher'a aynı pencerede gömüldü (SSH motoru gibi). Process Dump modu gerçek bir process (notepad) üzerinde test edildi, çalıştı. Full mod yazıldı ama Yönetici + test-signing gerektirdiği için henüz gerçek makinede test edilmedi.
- Dosya/klasör seçme eklendi (`file_acquirer.py`) — `ssh_engine` ekranında "Ne Alınacak?" seçimiyle Tam Disk / Dosya ya da Klasör arasında geçiliyor. Tek dosya ya da tüm klasör (recursive, `find -type f`) alınabiliyor, her dosya `sha256sum` ile yazmadan önce doğrulanıyor, goreli klasör yapısı korunuyor, `manifest_files.json` üretiliyor. Write-blocker bu modda uygulanmıyor (dosya seviyesinde anlamsız, her zaman Live gibi çalışır). Mock SSH ile hem tek dosya hem klasör senaryosu, GUI'nin gerçek worker thread'i üzerinden uçtan uca test edildi.
- Varsayılan tema koyu yapıldı (`shared/theme.py` + launcher + her iki motorun standalone girişi).
- Windows hedefe SSH ile disk + dosya/klasör alma eklendi (`windows_acquirer.py`) — `gui_v2.py`'ye "Hedef İşletim Sistemi" (Linux/Windows) seçici eklendi. Windows tarafı `dd`/`sha256sum`/`find` yerine PowerShell (`Get-Disk`, `Get-FileHash`, `Get-ChildItem`) kullanıyor; ham veri SSH metin kanalından bozulmadan geçmesi için Base64 ile taşınıyor. `concatenate_blocks`/`local_master_hash` (image_acquirer.py) Linux ile ortak, aynen kullanılıyor. Güvenlik: `disk_number` her yerde `int()`'e zorlanıyor, `remote_path` her yerde `powershell_quote()` (shlex.quote'un PowerShell karşılığı) ile kaçırılıyor + `-LiteralPath` kullanılıyor. Mock SSH ile disk imajlama, dosya/klasör alma ve enjeksiyon denemesi ayrı ayrı test edildi, GUI'den uçtan uca (gerçek worker thread'i üzerinden) da doğrulandı. Linux (RAM hariç) tarafı Windows disk sembolik simetri karşılığı sağlandı; Linux tarafında RAM alma (LiME vb.) bilinçli olarak askıya alındı (kernel-versiyonuna özel derleme gerektirdiği için ayrı, zor bir iş).
- Windows disk almada bağlantı-koptu/resume döngüsü eklendi — `windows_acquirer.py` artık Linux'taki `ensure_connection` fonksiyonunu (OS'tan bağımsız, aynen) kullanıyor; bağlantı 3 denemede toparlanamazsa `resume_from` ile döner, `gui_v2.py`'de kullanıcıya "tekrar bağlanıp devam edilsin mi?" soruluyor. Mock testle bağlantı kopması + devam senaryosu uçtan uca doğrulandı.
- Ortak rapor formatı eklendi — `shared/forensic_report.py` (yeni `ForensicReport` sınıfı) artık SSH motorunun disk + dosya/klasör modları (hem Linux hem Windows) ve RAM motorunun process + full modları için AYNI `report.json` şemasını üretiyor: vaka no, inceleyen, hedef (OS/host/kaynak), alma yöntemi ve zamanları, write-blocking durumu, SHA-256/boyut/chunk bilgisi, sonuç durumu, başarısız öğeler, doğrulama sonucu ve tam chain-of-custody olay listesi (`chain_of_custody.py`'ye yeni `read_events()` fonksiyonu eklendi, zaman aralığına göre filtreleyerek log dosyasından okur). `gui_v2.py`'ye "Vaka Bilgileri" kartı, `ram_gui.py`'de aynı alan artık her iki modda da (öncesinde sadece full modda) geçerli. RAM process modunda hash'i biz hesaplıyoruz (CLI üretmiyor), full modda RamImagerCLI'nin kendi ürettiği `.img.json`'daki hash/boyut okunup rapora taşınıyor. Mock/gerçek senaryolarla test edildi.
- `gui_v2.py` kaydırma düzeltmesi — "Vaka Bilgileri" kartı eklenince toplam içerik sabit pencere boyunu (`900x780`) aştı, log kutusu ve butonlar ekranın altında kaybolup kaydırma imkanı da yoktu. Başlık dışındaki her şey artık bir `CTkScrollableFrame` (`self.content`) içinde; fare tekerleği/kaydırma çubuğuyla erişilebiliyor. Headless testte scrollregion'ın görünür alandan büyük olduğu doğrulandı.
- **Bağlantı yöntemi seçimi + Tor Hidden Service (acil durum kapısı)** eklendi. Eski plan (madde 1'deki relay/rendezvous sunucusu, 6 haneli ID + PAKE) terk edildi — barındırma maliyeti/sorumluluğu gerektiriyordu. Yerine iki yöntem netleşti:
  - **Doğrudan / Port Yönlendirme** (birincil, çoğu vakayı kapsıyor): operatörün hedef ağın router'ına erişimi olduğu durumlar. Yeni kod gerekmedi — mevcut Host/Port alanları zaten bunu yapıyor, `gui_v2.py`'ye sadece bir "Bağlantı Yöntemi" seçici eklendi.
  - **Tor Hidden Service** (acil kapı, SADECE hedef ağda hiçbir erişim/yetki olmadığı en zor durum için): `shared/onion_auth.py` (operatör icin x25519 anahtar çifti üretimi, Tor'un ClientAuthV3 base32 formatı), `engines/portable_kit/tor_manager.py` (hedef cihazda gömülü Tor'u başlatıp SSH'ı `.onion` adresi olarak yayımlayan, sadece operatörün anahtarına açık bir v3 Hidden Service kuran modül — `stem` kütüphanesiyle), `engines/ssh_engine/local_collector/tor_client.py` (operatör tarafında Tor'u SOCKS proxy modunda başlatıp `ONION_CLIENT_AUTH_ADD` ile özel anahtarını tanıtan modül), `engines/ssh_engine/local_collector/socks5.py` (paramiko'yu `.onion` adresine bağlamak için elle yazılmış minimal SOCKS5 istemcisi — PySocks gibi ek bağımlılık eklenmedi). `ssh_connector.py`'ye `socks_proxy_port` parametresi eklendi (mevcut düz SSH yolu hiç değişmedi, ayrı bir mod). `chain_of_custody.py`'ye yeni `TOR_CONNECTION_ESTABLISHED` olayı eklendi. `gui_v2.py`'de "Tor (Acil Durum)" seçilince operatörün açık anahtarı otomatik üretilip (`keys/operator_tor_key.json`, `.gitignore`'da, asla commit edilmez) "Kopyala" butonuyla gösteriliyor. `.onion` adresinin operatöre iletimi: QR kod DEĞİL (delil cihazında ekstra uygulama/bağımlılık istemiyoruz) — sahadaki kişi kendi telefonuyla ekran görüntüsü/foto çekip kendi mesajlaşma kanalıyla gönderiyor (delil cihazının kendi ağı/uygulamaları hiç kullanılmıyor). Gömülü Tor binary'si (resmi Tor Project "Expert Bundle", Windows x86_64, sürüm 15.0.21) `shared/bin/tor/windows/tor.exe` altına eklendi — hem portable_kit hem ssh_engine AYNI binary'yi `shared/tor_binary.py` üzerinden buluyor (yol koda gömülü değil, `CHAMELEON_TOR_BINARY` ortam değişkeniyle override edilebilir, ileride binary güncellemesi sadece dosya değişikliği gerektirir). Test: her modül mock'landı (sahte Tor süreci/controller, sahte SOCKS5 sunucusu) ve `gui_v2.py` üzerinden uçtan uca doğrulandı; `tor.exe --verify-config` ile üretilen torrc'nin geçerli olduğu ayrıca kontrol edildi. **Gerçek Tor ağı üzerinden iki cihaz arası canlı test henüz yapılmadı** (kullanıcı ile birlikte yapılacak, madde 1'e taşındı).
- Açılış (splash) ekranı eklendi — kullanıcının Oxygen Forensic Detective'te gördüğü tarzda, `launcher/chameleon_gui.py` artık uygulama açılırken ~1.8sn (ya da tıklanınca hemen) bir splash gösterip sonra ana seçim ekranına geçiyor. Kullanıcının verdiği marka logosundan (`C:\Users\yasar\Desktop\duvar\...png`) SADECE bukalemun ikonu (yazı/wordmark olmadan) kırpılıp arka planı şeffaflaştırılarak [shared/assets/chameleon_icon.png](../shared/assets/chameleon_icon.png) olarak eklendi (kırpma/şeffaflaştırma bir kerelik Pillow ile yapıldı, uygulamanın kendisi Pillow'a bağımlı DEĞİL — PNG'yi tkinter'ın yerleşik `PhotoImage`'i okuyor). Sürüm numarası tek bir yerden yönetiliyor: [shared/version.py](../shared/version.py) (`VERSION = "0.1.0"`). Ekranda ikon + "CHAMELEON" + alt başlık + sürüm + telif satırı var, tema rengini (`shared/theme.py`) kullanıyor. Gerçek pencere açılıp ekran görüntüsü alınarak (mock değil) doğrulandı.
- `shared/i18n/strings.py`'deki `ssh_engine_desc` metni güncellendi — Windows hedef desteği eklendiğinden beri "Linux sunucuya SSH ile bağlanıp dd/sha256sum ile çalışır" yazıyordu (artık yanlış/eksikti), "Linux veya Windows hedefe SSH ile bağlanıp disk/dosya imajı alır" oldu (TR+EN).
- Chunk (blok) boyutu ayarlanabilir hale getirildi — `image_acquirer.acquire_disk_image` ve `windows_acquirer.acquire_disk_image_windows` zaten `block_size_mb` parametresi alıyordu, sadece `gui_v2.py`'de sabitti. "Hedef Disk ve İşlem Modu" kartına 4/16/32/64 MB seçenekli bir "Blok Boyutu" seçici eklendi (`self.block_size_var`, varsayılan 4 MB), hem Linux hem Windows worker thread'lerine değer olarak geçiriliyor. Yarım kalan bir manifestten devam edilirken (resume) seçim kutusu değil, manifestteki orijinal `block_size_mb` kullanılıyor — aksi halde parçalar farklı boyutlarda karışıp `concatenate_blocks` bozulurdu. Mock SSH ile 16 MB blok boyutu uçtan uca doğrulandı (2x16MB blok, hash eşleşti).
- **Launcher, Oxygen Forensic Device Extractor tarzı sol sidebar navigasyona geçirildi** — `chameleon_gui.py` artık tek bir düz seçim ekranı değil, kalıcı bir sol menü (Ana Sayfa / SSH ile Uzak İmaj Al / RAM İmajı Al / Ayarlar) + sağda değişen içerik alanından oluşuyor. Dil/tema geçişi "Ayarlar" sayfasına taşındı. Her yöntemin artık kendi TANITIM SAYFASI var (`METHOD_INFO` sözlüğü, TR+EN): ne işe yaradığı, ne zaman kullanılacağı, gerekenler ve adım adım kullanım — Oxygen'deki "Methods" ekranlarına benzer şekilde, "Başlat" butonuna basınca gerçek araç açılıyor. Pencere/görev çubuğu ikonu da artık varsayılan Tk ikonu değil, bukalemun logosu (`shared/assets/chameleon.ico`, `iconbitmap` ile). `ssh_engine/gui_v2.py`'deki Tor (Acil Durum) açıklaması da çok kısaydı — "nedir", "kullanım adımları" (numaralı, 5 adım) ve "bağlantı sonrası ne değişir" bölümleriyle ilk kez kullanan biri için yeterli olacak şekilde genişletildi. Gerçek pencere açılıp ekran görüntüsü alınarak (mock değil) doğrulandı; `docs/oturum_ozeti.md` bu oturumun tam özetini tutuyor.
- **VPN bağlantı yöntemi + Cihaz Sahibi/Yetkili Kişi alanı + rapor sunumu + Vaka Geçmişi** eklendi:
  - "Bağlantı Yöntemi" kartına üçüncü seçenek olarak **VPN** eklendi — kod tarafında "Doğrudan" ile birebir aynı yolu izliyor (operatör zaten VPN tünelinde olduğu için ekstra bir taşıma katmanı gerekmiyor), sadece `chain_of_custody.py`'ye yeni `VPN_CONNECTION_USED` olayıyla ve raporun `connection_method` alanıyla hangi ağ yolunun kullanıldığı ayrıca kayıt altına alınıyor.
  - Vaka Bilgileri kartına **Cihaz Sahibi / Yetkili Kişi** alanı eklendi (hem `gui_v2.py` hem `ram_gui.py`) — adli bilişimde "kim inceledi" kadar "cihaz kimden/kimin izniyle alındı" bilgisi de delil zinciri için önemli. `shared/forensic_report.py`'ye `custodian` alanı olarak işleniyor.
  - `ForensicReport` artık `report.json`'un yanında **`report.html`** de üretiyor (`to_html()`) — tüm vaka bilgileri, bütünlük/hash, sonuç VE tam chain-of-custody olay listesiyle birlikte, yazdırılabilir/paylaşılabilir tek sayfa. İşlem başarıyla bitince (`gui_v2.py` + `ram_gui.py`) artık sadece loga yazmıyor, **okunabilir bir özet penceresi** açıyor ("Raporu Aç (HTML)" butonuyla).
  - Her rapor kaydedildiğinde, motor/oturum fark etmeksizin ortak bir listeye (`shared/data/case_history.json`, `.gitignore`'da) de ekleniyor; launcher'da yeni **"Vaka Geçmişi"** sidebar sayfası bu listeyi okuyup tüm geçmiş imajları (vaka no, motor, hedef, inceleyen, yetkili kişi, durum, "Raporu Aç" butonu) gösteriyor.
  - Test: `forensic_report.py` (custodian/connection_method/HTML/geçmiş listesi doğru mu), `gui_v2.py` (VPN paneli görünürlüğü, rapor özeti penceresi crash etmiyor mu) mock/headless testlerle, "Vaka Geçmişi" sayfası gerçek pencere + örnek 3 kayıtla ekran görüntüsü alınarak doğrulandı.
- **Tek tıkla kurulum (PyInstaller ile `.exe`)** tamamlandı — proje kökünde elle yazılmış [build.spec](../build.spec) ile `pyinstaller build.spec` çalıştırılınca tek bir `dist/Chameleon.exe` (~28MB, Windows) üretiliyor. Zorluk: `ssh_engine`/`ram_engine` modülleri (`gui_v2.py`, `ram_gui.py` vb.) çalışma anında `sys.path`'e eklenip düz `import` ile yükleniyor (PyInstaller'ın normalde otomatik bulamayacağı bir desen) — bunun için `shared/`, `engines/ssh_engine/local_collector/`, `engines/ram_engine/{ram_gui.py,cli,driver}` KAYNAK AĞACIYLA BİREBİR AYNI GÖRECELİ YERLEŞİMDE veri (data) olarak paketleniyor, böylece mevcut `sys.path.insert()+import` deseni hiç değişmeden çalışmaya devam ediyor. `launcher/chameleon_gui.py`'deki `PROJECT_ROOT` hesaplaması donmuş (frozen) modda `sys._MEIPASS`'a bakacak şekilde güncellendi (tek kod değişikliği). `customtkinter`'ın kendi PyInstaller hook'u olmadığı için tema/font/ikon dosyaları `collect_data_files("customtkinter")` ile elle toplanıyor. Test: gerçek `.exe` çalıştırılıp (a) çökmediği, (b) `sys._MEIPASS` altında gömülü `tor.exe`/`gui_v2.py`/`RamImagerCLI.exe`'nin doğru yerde olduğu, (c) ekran görüntüsüyle SSH tanıtım sayfasının doğru render edildiği doğrulandı.
- **İki üretim hatası bulundu ve düzeltildi** (detaylar: [docs/hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)):
  1. Derlenmiş `.exe`'de "SSH motoru yüklenemedi" hatası — `gui_v2.py`'nin veri dosyası olarak yüklenmesi yüzünden PyInstaller onun `tkinter.scrolledtext/messagebox/filedialog` ihtiyacını statik analizle göremiyordu. `build.spec`'e `hiddenimports` olarak eklendi.
  2. Pencere ikonu bukalemun logosuna hiç dönmüyordu — sebebi `customtkinter`'ın kendisinin, `iconbitmap()` (kendi izlediği metot) çağrılmazsa 200ms sonra kendi varsayılan logosunu basması. `iconphoto()` bu izlemeyi tetiklemediği için sessizce üstüne yazılıyordu. Çözüm: `self.root.iconbitmap(...)` çağırmak (splash ekranı için de aynı düzeltme).
- **`customtkinter` → `PySide6` tam geçiş** tamamlandı — verilen tasarım sistemine (hex renk paleti, Inter/JetBrains Mono, 4-6px köşe, gölgesiz, özel radyo/checkbox, 4 durumlu buton, nokta+metin durum rozeti) uygun yeni bir bileşen kütüphanesi (`shared/ui_kit/`: `theme_qt.py`, `widgets.py`, `icons.py`, `fonts.py`) eklendi. Backend/iş mantığına (SSH, Tor, kripto, RAM motoru subprocess çağrıları, PowerShell/bash komutları) HİÇ dokunulmadı — sadece UI katmanı değişti. Thread-güvenli UI güncellemesi artık `root.after(0, ...)` yerine Qt sinyal/slot; `_ask_yesno`'nun worker-thread-içinden-bloke-eden diyalog deseni `threading.Event` + worker'ın kendi instance attribute'larıyla korundu (`Qt.BlockingQueuedConnection` + mutable dict sinyal argümanının PySide6'da referans değil KOPYA taşıdığı, bir mock testle keşfedildi — bkz. `docs/hatalar_ve_sonuclar.md`). Üç ekran de (`launcher/chameleon_gui.py`, `engines/ssh_engine/local_collector/gui_v2.py`, `engines/ram_engine/ram_gui.py`) canonical isimlere taşındı, eski CTk dosyaları (`shared/theme.py` dahil) silindi. `build.spec` PySide6'ya göre yeniden yazıldı.
- **Launcher'da dil (TR/EN) + tema (koyu/açık) desteği tamamlandı** — Ayarlar sayfasındaki geçiş kartları artık gerçekten çalışıyor: `METHOD_INFO` sözlüğündeki dört yöntemin (Doğrudan, VPN, Tor, RAM) TÜM alanları (`title`/`what`/`when`/`requires`/`steps`/`warning`) için İngilizce çeviri eklendi (önceden sadece TR vardı, dil `en` iken yöntem sayfası açılınca `KeyError` verirdi); ayrıca ana sayfa, yöntem tanıtım sayfalarındaki bölüm başlıkları/butonlar ve Vaka Bilgileri ekranındaki tüm etiket/buton metinleri de `self.lang` bayrağına göre TR/EN seçiliyor. Qt widget'ları renkleri kurulum anında QSS'e gömdüğü için tema/dil değişince ilgili ekran yeniden kuruluyor (`_apply_theme`/`_apply_lang` → `_build_shell()` + geçerli sayfa). Headless (`QT_QPA_PLATFORM=offscreen`) bir test scriptiyle her dört sayfa hem TR hem EN'de, hem koyu hem açık temada exception fırlatmadan gezildi; `widget.grab()` ile alınan ekran görüntüleriyle görsel olarak da doğrulandı (sidebar/ana sayfa/ayarlar/Tor sayfası). `gui_v2.py`/`ram_gui.py`'nin kendi araç ekranları hâlâ TR-only (bilinçli kapsam sınırı, bkz. "Daha sonra").
- **Mevcut ekranlarda görsel tutarlılık düzeltmeleri** yapıldı (detaylar: [docs/hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)):
  - "Vaka Bilgileri" bağımsız adım sayfası artık diğer sayfalardaki gibi tek bir karta (`widgets.Card`, max 640px, üstte sola yaslı) alındı — önceden 3 giriş alanı kartsız, boş koyu arka plan üzerinde duruyordu.
  - "Doğrudan/Port Yönlendirme" (ve diğer üç yöntemin) bilgi sayfasındaki "Ne zaman kullanılır?"/"Gerekenler"/"Adım adım kullanım" blokları artık ayrı birer kart — diğer ekranlarla birebir aynı stil (arka plan, kenarlık, padding, köşe, mavi uppercase başlık). Numaralı listelerdeki rakamlar, yeni `widgets.StepBadge` bileşeniyle (mavi daire içinde beyaz rakam) değiştirildi, wizard/rehber hissi güçlendirildi.
  - `widgets.StatusBadge` düz nokta+metinden gerçek bir pill/rozete çevrildi — durum renginin soluk tonunda arka plan + aynı renkte nokta + aynı renkte kalın metin (bağlı=yeşil, bağlı değil=gri, hata=kırmızı, bağlanıyor=sarı). Yeni `_tint()` yardımcı fonksiyonu (Qt QSS'de alfa 0-255 aralığında, CSS'teki 0-1'den farklı — bkz. `docs/ogrenilenler.md`) ile hesaplanıyor.
  - `gui_v2.py`'de "Bağlan ve Diskleri Listele" butonuna gerçek bir yükleniyor durumu eklendi: tıklanınca buton devre dışı kalıp "Bağlanıyor..." yazıyor, rozet sarıya dönüyor; bağlantı başarılı olursa yeşil, hata alırsa kırmızıya geçiyor (önceden hata durumunda sessizce gri/"bağlı değil"e dönüyordu). Host/Port/SSH Anahtar alanlarının zaten `MonoInput` (JetBrains Mono) kullandığı, şifre alanının zaten maskelendiği (`EchoMode.Password`) doğrulandı — ek değişiklik gerekmedi.
  - Düzeltme sırasında bir kart-içi satır sarmalama hatası (`row_w = QWidget()` ile arka plan sızıntısı) bulunup giderildi — bkz. `docs/hatalar_ve_sonuclar.md`.
  - Artık kullanılmayan `SectionHeading` sınıfı (yerini `Card`'ın kendi başlığı aldı) kaldırıldı.
- **Güvenlik denetimi ve iki bulgunun düzeltilmesi** (SSH kimlik bilgileri, Tor x25519 anahtarları, `RamImagerCLI.exe`/PowerShell subprocess çağrıları, hardcoded secret taraması):
  - `ram_gui.py`'de "Full" (Yönetici/UAC) modda `ShellExecuteW`'e giden parametre string'i sadece boşluk varsa tırnaklıyordu, içindeki `"` karakterini kaçırmıyordu (CWE-88, argüman enjeksiyonu) — Vaka No/İnceleyen gibi serbest metin alanlarından yükseltilmiş sürece ekstra argüman sızabiliyordu. `subprocess.list2cmdline()` ile değiştirildi (normal girdilerde çıktı birebir aynı, sadece `"` içeren girdilerde artık doğru kaçırılıyor).
  - Operatörün Tor özel anahtarı (`keys/operator_tor_key.json`) düz metin JSON olarak yazılıyordu, dosya izni hiç kısıtlanmıyordu — `gui_v2.py`'ye `_restrict_key_file_permissions()` eklendi (POSIX'te `chmod 600`, Windows'ta `icacls` ile sadece mevcut kullanıcıya erişim).
  - Diğer bulgular (SSH parolasının komut satırına hiç gömülmemesi, host key doğrulamasının varsayılan `RejectPolicy` olması, tüm uzak yolların `shlex.quote`/`powershell_quote` ile kaçırılması) zaten doğruydu, değişiklik gerekmedi.
- **"Uzak Yol" alanına "Gözat" (uzak klasör gezgini) eklendi** — dosya/klasör modunda önceden yol elle yazılıyordu (native `QFileDialog` bu bilgisayarın diskini gösterir, SSH hedefini gösteremez). Yeni `RemoteBrowseDialog` (gui_v2.py), mevcut SSH bağlantısı üzerinden salt-okunur `find`/`Get-ChildItem` ile klasör klasör gezinmeyi sağlıyor — `file_acquirer.list_remote_directory`/`windows_acquirer.list_remote_directory_windows` (her ikisi de tek seviye, `-maxdepth 1`/`Get-ChildItem` bir seviye, hiçbir şey yazmıyor). Her klasör açılışı chain-of-custody'ye yeni `DIRECTORY_LISTED` olayı olarak ayrıca loglanıyor (operatörün hedefte tam olarak nereye baktığı izlenebilsin diye) — bu, forensic-soundness açısından zarar değil kazanç: önceden elle yazılan bir yolun hiçbir izi yoktu, artık gezinme adım adım kayıt altında. Klasöre çift tıklayınca içine giriyor, dosyaya çift tıklayınca seçip kapanıyor, "Bu Klasörü Seç" mevcut klasörü seçiyor. Mock SSH ile Linux+Windows listeleme, gezinme, chain-of-custody loglaması ve "bağlantı yokken Gözat" hata durumu test edildi; ekran görüntüsüyle görsel olarak da doğrulandı.
- **SSH bağlantı hataları artık nedene özgü + Sunucu Kimlik Doğrulama seçeneği** — `ssh_connector.py`'nin `connect()`'i artık başarısızlık türünü sınıflandırıyor (`last_error_type`: `host_key`/`auth`/`unreachable`/`other`), `gui_v2.py` her biri için ayrı, çözüm gösteren bir mesaj üretiyor (önceden tek bir genel "bilgileri kontrol edin" mesajı vardı). Kök sebep: uygulama `RejectPolicy` kullandığı için daha önce hiç bağlanılmamış bir sunucuya (adli bilişimde asıl senaryo) bilgiler doğru olsa bile bağlantı reddediliyordu, arayüzde bunu onaylatacak bir yol da yoktu. Çözüm olarak "SSH Bağlantı Bilgileri" kartına **"Sunucu Kimlik Doğrulama: Sıkı doğrula (önerilen) / Doğrulamayı atla"** seçeneği eklendi (varsayılan güvenli tarafta) — atlanırsa `chain_of_custody.py`'ye yeni `HOST_KEY_VERIFICATION_SKIPPED` olayıyla ayrıca kaydediliyor. Gerçek bir SSH sunucusuna (WSL Ubuntu) karşı üç hata türü ve düzeltmenin kendisi ayrı ayrı test edildi.
- **Bilgi Merkezi sayfası eklendi** — `shared/help_content.py` (tek, paylaşılan içerik kaynağı) + launcher sidebar'ına yeni "Bilgi Merkezi" sekmesi. Kafa karıştırıcı olabilecek 6 kavram için detaylı anlatım var: Sunucu Kimlik Doğrulama (Host Key), Live/Offline Acquisition (Yazma Engelleme), Delil Zinciri (Chain of Custody), Hash/Bütünlük Doğrulaması, RAM "Full" Modu (Yönetici/Sürücü/Secure Boot), Tor/.onion/Operatör Anahtarı. İlgili ekranlardaki ("SSH Bağlantı Bilgileri", "Hedef Disk ve İşlem Modu", "İşlem Logu", RAM "Full RAM" kartı, launcher'daki Tor tanıtım sayfası) her "Bu ne demek?" linki, launcher içinden açıldığında Bilgi Merkezi'ndeki ilgili karta doğrudan kaydırıyor (`QTimer.singleShot(0, ...)` ile, scroll alanı boyutlandıktan sonra); `gui_v2.py`/`ram_gui.py` standalone çalıştırıldığında (Bilgi Merkezi sayfası yoksa) aynı içerik küçük bir dialogda gösteriliyor. Headless testlerle her linkin doğru karta kaydırdığı ekran görüntüsüyle doğrulandı. **Sonradan bulunan bir bug**: bir arac ekranındaki linkten gidince o ekrandaki form/bağlantı siliniyordu, kullanıcı geri bildirdi — düzeltildi (`_show_help_from_tool` + `_return_from_help`, arac ekranı silinmeden saklanıp "Geri dön" ile aynen geri getiriliyor; detaylar [hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)'da).
- **`cryptography` paketi güvenlik güncellemesi** — `pip-audit` taramasında bulunan PYSEC-2026-3552 (PKCS#7 EnvelopedData şifre çözmede Bleichenbacher oracle) için 49.0.0 → 50.0.1'e yükseltildi. Projede sadece `onion_auth.py`'de x25519 anahtar üretimi için kullanıldığından etkilenen kod yolu zaten devrede değildi, yine de bedelsiz bir güncelleme olduğu için yapıldı; Tor anahtar üretimi regresyon testiyle doğrulandı.
- **Rol seçim ekranı + hedef taraf sihirbazı eklendi** — uygulama artık her açılışta (kayıtlı bir tercih yok, her seferinde farklı biri kullanıyor olabilir) önce **"Bu bilgisayardaki kişi kimsiniz? Operatörüm / Bu Cihaz İnceleniyor"** soruyor (`chameleon_gui.py`, `_show_role_select`). "Operatörüm" mevcut launcher'ı hiç değişmeden açıyor. "Bu Cihaz İnceleniyor" seçilirse -- yani sahadaki, teknik bilgisi olmayabilecek kişi kullanıyorsa -- sidebar/RAM-imajı/SSH-araçları gibi operatör araç seti HİÇ GÖSTERİLMEZ, bunun yerine `_show_target_wizard` ile 3 numaralı adımdan oluşan ayrı bir sihirbaz açılıyor: (1) operatör anahtarını yapıştır, (2) "Bağlantıyı Başlat" (embedded Tor'u ayağa kaldırıp hidden service kurar, `TargetKitWorker` ile arka planda -- dakikalar sürebildiği için UI donmaz), (3) üretilen `.onion` adresini kopyala/operatöre ilet + "Bağlantıyı Kapat". Pencere kapanırken/"Geri" ile çıkılırken (`closeEvent`, `_target_cleanup`) açık kalmış bir Tor süreci varsa otomatik kapatılıyor -- orphan process kalmıyor, gerçek Tor süreciyle (tasklist kontrolüyle) doğrulandı. Bu, madde 1'deki "tek, dağıtılabilir kit paketi" ihtiyacının GUI kısmını büyük ölçüde karşılıyor (öncesinde `tor_manager.py` hiçbir arayüzü olmayan çıplak bir scriptti).
- **Kritik bug bulundu ve düzeltildi: `tor_manager.py`'de `ADD_ONION` hiç çalışmıyordu** — hedef taraf sihirbazı gerçek gömülü Tor'a karşı ilk kez test edilirken ortaya çıktı (bkz. bir alt madde: "gerçek Tor ağı üzerinden canlı test"). `create_ephemeral_hidden_service()` çağrısında `key_type="ED25519-V3"` verilmişti; stem bunu `key_content` (varsayılan `"BEST"`) ile birleştirip Tor'a `ADD_ONION ED25519-V3:BEST` gönderiyordu -- Tor bunu "ED25519-V3 türünde, HAZIR bir anahtar, içeriği 'BEST' metni" sanıp decode etmeye çalışıp reddediyordu ("Failed to decode ED25519-V3 key"). Doğrusu: `key_type` varsayılanında (`"NEW"`) kalmalı, `key_content="ED25519-V3"` verilmeliydi ("yeni bir ED25519-V3 anahtarı SEN üret" anlamına geliyor). Düzeltildi, gerçek bir operatör anahtarıyla tekrar denendi -- geçerli bir `.onion` adresi üretildi.
- **Gerçek Tor ağı üzerinden canlı test -- hedef taraf kısmı tamamlandı**: yukarıdaki bug düzeltmesiyle birlikte, hedef tarafın (portable kit) gömülü Tor'u başlatıp operatörün anahtarına açık bir v3 hidden service kurması artık gerçek Tor ağına karşı çalışıyor ve doğrulandı (mock değil). **Eksik kalan**: operatörün, üretilen o gerçek `.onion` adresine `tor_client.py` ile bağlanıp SOCKS5 üzerinden SSH kurabildiği -- yani döngünün "iki cihaz arası" ikinci yarısı -- henüz denenmedi (muhtemelen iki ayrı makine/ağ gerektiriyor, kullanıcıyla birlikte yapılacak).
- **4 uzman ajanla (Incident Responder/adli bilişim, Security Architect, Penetration Tester, Code Reviewer) kapsamlı inceleme yapıldı, bulunan tüm gerçek sorunlar düzeltildi**:
  - **[KRİTİK] Rapordaki "doğrulandı" alanı GUI akışında hiç doldurulmuyordu.** `report.save()` işlem bitince hemen çalışıyor, "İmaj Doğrula" sorusu ondan SONRA soruluyordu -- operatör doğrulasa bile bu, `report.json`/`report.html`'e hiç yansımıyordu (`ForensicReport.set_verification()` GUI'de hiç çağrılmıyordu; sadece CLI'nin, GUI'de kullanılmayan bir yolunda vardı). `gui_v2.py`'ye `_last_report`/`_last_report_path` eklendi, `VerifyWorker` artık dogrulama sonucunu (`set_verification()`) rapora işleyip `report.save()`'i TEKRAR çağırıyor. `set_verification()`, dogrulama zamanının rapor penceresine (start/end_time_utc) girmesi için `end_time_utc`'yi de genişletiyor -- aksi halde `HASH_VERIFIED` olayı raporun kendi olay listesine hiç girmezdi. `forensic_report.py`'nin `_append_to_history()`'si de aynı `report_path` için ikinci kez çağrılınca artık Vaka Geçmişi'ne DUPLİKE satır eklemek yerine mevcut satırı güncelliyor. Gerçek bir dosya + gerçek SHA-256 ile uçtan uca test edildi.
  - **[KRİTİK] Delil zinciri log'u, hedef cihazdaki dosya adlarıyla manipüle edilebiliyordu.** `chain_of_custody.py` satırları `|` ile ayırıyor; `description` çoğu zaman HEDEF cihazdaki (incelenen tarafın kontrolündeki) dosya adlarından geliyor. İçinde `|` olan bir dosya adı, o KAYDIN parçalama sayısı uyuşmadığı için `read_events()` tarafından sessizce DÜŞÜRÜLMESİNE (delil kaybı); içine satır sonu + sahte bir `[ts] | EVENT | .. | hash` deseni yerleştirilirse SAHTE bir log kaydı ENJEKTE EDİLMESİNE yol açıyordu. Yeni `_sanitize_log_field()` ile `|` görsel olarak benzer ama farklı bir sembolle (`¦`), satır sonları boşlukla değiştiriliyor. Her iki saldırı senaryosu da (kayıp + sahte kayıt) test edilip artık engellendiği doğrulandı.
  - **[GÜVENLİK] Hedef sihirbazında yapıştırılan operatör anahtarı hiç doğrulanmıyordu.** Sahadaki kişi yanlış/saldırgan bir anahtar yapıştırsa arayüz bunu ayırt etmiyordu. `shared/onion_auth.py`'ye `key_fingerprint()` eklendi -- hem operatör ekranında (kendi anahtarının yanında) hem hedef sihirbazında (yapıştırılan anahtarın yanında) AYNI kısa kod (örn. "3F2A-9C11-88DE") canlı gösteriliyor, sahadaki kişi başlatmadan önce bunu telefonla operatöre okuyup teyit edebiliyor.
  - **[GÜVENLİK] Tor kontrol protokolü komutlarına ham metin enjekte edilebiliyordu.** `tor_client.py`'nin `ONION_CLIENT_AUTH_ADD` ve `tor_manager.py`'nin `ClientAuthV3=` değerleri (biri dosyadan okunan, diğeri GUI'de doğrudan YAPIŞTIRILAN serbest metin) hiç format doğrulamasından geçmeden Tor'un ham control-protokol komutuna yerleştiriliyordu -- CR/LF içeren bozuk/kötü niyetli bir değer teorik olarak ikinci bir komut enjekte edebilirdi. `onion_auth.py`'ye `is_valid_key_b32()` eklendi (52 karakter, base32 formatı tam eşleşmeli), her iki modül de Tor'a gitmeden önce bunu kontrol ediyor; `tor_client.py`'ye ayrıca `.onion` adresi için de (56 karakter) aynı kontrol eklendi. Gerçek Tor ile regresyon testi geçti.
  - **[GÜVENLİK, düşük öncelik] `chameleon_known_hosts` sembolik link koruması + eşzamanlı TOFU kaydı yarış durumu.** Dosya yolunda bir symlink varsa artık erken ve açıkça hatayla durduruluyor. Aynı süreç içindeki eşzamanlı `strict=False` bağlantılarının known_hosts dosyasını üstüne yazarak birbirinin öğrendiği anahtarı kaybetmesini önlemek için `_TOFU_SAVE_LOCK` eklendi.
  - **[SAĞLAMLIK] `last_error_type` sınıflandırması artık paramiko'nun hata METNİNE değil (kırılgan, iç detay), yeni bir `_UnknownHostKeyError`/`_StrictPolicy` çiftiyle exception TÜRÜNE bakıyor.**
  - **[İYİLEŞTİRME] Delil zincirine artık sadece "öğrenildi/biliniyor" değil, sunucunun GERÇEK host key parmak izi de (`ssh.host_key_fingerprint`) kaydediliyor** -- ileride bağımsız bir kaynaktan alınacak bir parmak iziyle karşılaştırılabilsin diye.
  - **[Adli bilişim] Live Acquisition'ın "tek bir ana ait dondurulmuş bir kopya değildir" sınırlaması artık hem Bilgi Merkezi'nde hem raporun `write_blocking_reason` alanında açıkça belirtiliyor** (önceden hiçbir yerde yazmıyordu).
  - **[Adli bilişim] Bağlantı koptuktan sonra devam edilen (resume) işlemlerde artık write-block durumu KÖRÜ KÖRÜNE "hâlâ geçerlidir" varsayılmıyor, `blockdev --getro`/`Get-Disk.IsReadOnly` ile GERÇEKTEN kontrol edilip sonucu delil zincirine kaydediliyor** (`write_block_helper.is_write_blocked`, `windows_acquirer.is_write_blocked_windows`) -- hedef resume sırasında yeniden başlatılmışsa (write-block kalıcı değil) bu artık fark ediliyor. Mock SSH ile test edildi.
  - **[Sihirbaz kararlılığı]** Hedef sihirbazından bağlantı kurulurken "Geri" ile çıkılırsa, gecikmeli sonuç artık silinmiş widget'lara dokunup çökertmiyor (sinyaller koparılıp otomatik kapatmaya yeniden bağlanıyor); "Bağlantıyı Başlat"a bağlıyken tekrar basmak artık ikinci bir bağlantı denemesi başlatmıyor; Bilgi Merkezi'nde "Geri" kullanılmadan terk edilen bir araç ekranı, İÇİNDE hâlâ çalışan bir iş (örn. devam eden bir imaj alma) varsa artık sessizce SİLİNMİYOR (iş bitene kadar korunuyor).
  - Reddedilen bir öneri: hedef cihazın kendi makinesine de bir delil zinciri kaydı yazması -- kullanıcı haklı olarak reddetti, hedef cihaza herhangi bir yazma delil bütünlüğünü zedeler.
  - Ertelenen (roadmap'e not, mimari ölçekte, bu oturumda yapılmadı): delil zinciri log dosyasının kendi bütünlüğünün korunması (hash zinciri/imza -- şu an sonradan değiştirilebilir, fark edilmez), operatör Tor anahtarının hiç yenilenmemesi (tüm vakalar boyunca aynı anahtar), her sihirbaz çalıştırmasında yeni bir onion kimliği üretilmesi (TOFU'nun "tekrar bağlanınca tutarlılığı kontrol et" faydasını pratikte devre dışı bırakıyor).
- **Host key TOFU (hatırla + karşılaştır) eklendi** — "Doğrulamayı atla" modu önceden hiçbir hafızaya sahip değildi (`WarningPolicy`: her bağlantıda, aynı cihaza onuncu kez bağlanılsa bile, sorgusuzca kabul ediyordu). `ssh_connector.py`'ye Chameleon'a özel bir known_hosts dosyası eklendi (`engines/ssh_engine/keys/chameleon_known_hosts` -- kullanıcının kendi `~/.ssh/known_hosts`'unu hiç değiştirmiyor, `.gitignore`'daki `**/ssh_engine/keys/` zaten kapsıyor) ve yeni `_TofuPolicy` (paramiko `AutoAddPolicy` türetilmiş) ile: ilk bağlantıda sunucunun kimliği bu dosyaya otomatik kaydediliyor (`host_key_status = "learned"`), sonraki bağlantılarda o kayıtla karşılaştırılıyor (eşleşirse `"known"`, sessizce geçiyor). **Kritik**: sunucunun kimliği SONRADAN değişirse (paramiko'nun kendi davranışı, hem `strict=True` hem `strict=False` için -- policy'nin devreye girmesine bile gerek kalmadan) `BadHostKeyException` fırlatılıyor, "atla" seçili olsa bile bağlantı REDDEDİLİYOR (`last_error_type = "host_key_mismatch"`, kullanıcıya "DİKKAT: bu sunucunun kimliği daha önce bildiğimizden FARKLI" diye özel bir mesaj gösteriliyor). Bir kez TOFU ile öğrenilen bir sunucu, artık "Sıkı doğrula" modunda da çalışıyor (aynı dosyayı iki mod da okuyor). `chain_of_custody.py`'ye giden `HOST_KEY_VERIFICATION_SKIPPED` kaydı da "ilk kez öğrenildi" ile "önceden bilinen kayıtla eşleşti" ayrımını yapacak şekilde zenginleştirildi; Bilgi Merkezi'ndeki ilgili anlatım da yeni davranışı yansıtacak şekilde güncellendi. Gerçek bir SSH sunucusuna (WSL) karşı dört senaryo da (öğrenme, tanıma, sıkı modda geçerlilik, gerçekten değişen bir anahtarın reddedilmesi -- sunucunun host key'i bilerek yeniden üretilerek) ayrı ayrı test edildi.

- **Erişilebilirlik denetimi (`shared/ui_kit/` + tüm ekranlar) yapıldı, bulunan tüm sorunlar düzeltildi** — WCAG kontrast oranları matematiksel olarak hesaplandı (relative luminance formülü), kod satır satır okunarak (canlı test değil) klavye odağı/etkileşim eksikleri bulundu:
  - **[Kontrast]** Kart başlıkları (`ACCENT`, tüm uygulamada onlarca yerde) koyu temada 3.35–3.66:1 idi, WCAG AA'nın istediği 4.5:1'in altında. Yeni bir `ACCENT_TEXT` tokeni eklendi (`theme_qt.py` -- koyu temada `#3B82F6`, açık temada zaten yeterli olan `ACCENT` ile aynı) ve `Card` başlığı + tüm "Bu ne demek?"/parmak izi/Tor paneli başlıkları gibi küçük-metin kullanımları (7 ayrı yer, `gui_v2.py`/`chameleon_gui.py`/`ram_gui.py`) buna taşındı. `ACCENT`'in kendisi (buton dolgusu olarak) değişmedi.
  - **[Kontrast]** `BORDER` (girdi alanı kenarlığı, kart sınırı, radyo düğmesi boş halkası — TEK bir token, her yerde) koyu temada `BG_LAYER2`'ye karşı 1.2:1'di (WCAG'in UI bileşen sınırları için istediği 3:1'in çok altında) — bir form alanının odaksızken nerede başlayıp bittiği düşük görüşle ayırt edilemiyordu. `#30363D` → `#6B7280` (üç arka plana karşı da 3:1+). Açık temada da aynı sorun vardı, `#D1D5DB` → `#828A98`.
  - **[Kontrast]** Açık temada `WARNING` metni 2.97:1'di (RAM "Full mod" uyarısı) — `#D97706` → `#B45309` (4.68:1).
  - **[Klavye odağı — en etkili düzeltme]** `RadioButton.paintEvent()` klavye odağını HİÇ çizmiyordu (WCAG 2.4.7) — Tab ile gezinirken hangi seçenekte olduğunuz sadece işaretlemeyi değiştirdikten SONRA belli oluyordu. Artık `hasFocus()` iken kontrolün etrafına belirgin, yuvarlak köşeli bir çerçeve çiziliyor; `setFocusPolicy(StrongFocus)` de eklendi. Bağlantı Yöntemi, Sunucu Kimlik Doğrulama, Hedef İşletim Sistemi, Live/Offline, RAM modu, Dil/Tema seçimi — hepsi bunu kullandığı için tek bir düzeltme uygulama genelini kapsadı.
  - **[Klavye odağı]** `Input`/`MonoInput`'un odak göstergesi zayıftı (`ACCENT` kenarlık, `BG_LAYER2`'ye karşı 2.84:1, 3:1 eşiğinin altında) — `ACCENT_TEXT`'e (3.99:1) geçti, kenarlık kalınlığı odaklanınca 1px'ten 2px'e çıkıyor (dış kutu boyutu sabit kalsın diye padding 1px azaltılarak telafi edildi, sıçrama yok).
  - **[Klavye erişimi]** `MonoLabel` (işlem sonunda gösterilen SHA-256 hash gibi değerler) sadece fareyle seçilebiliyordu — `TextSelectableByKeyboard` + `FocusPolicy.StrongFocus` eklendi, artık klavye-only bir kullanıcı da kopyalayabiliyor.
  - **[Klavye odağı]** `PrimaryButton`/`SecondaryButton`'a `:focus` QSS durumu eklendi (öncesinde hover/pressed/disabled vardı, focus yoktu). `PrimaryButton` zaten mavi dolgulu olduğu için `ACCENT_TEXT` kenarlık neredeyse hiç seçilmiyordu (ölçüm: 1.41:1) — beyaz kenarlığa çevrildi (5.17:1).
  - Headless ekran görüntüleriyle hem koyu hem açık temada, hem odaklı hem odaksız durumda görsel olarak doğrulandı; önceki tüm regresyon testleri (form/bağlantı koruma, kaydırma, sihirbaz yaşam döngüsü) tekrar çalıştırılıp bozulma olmadığı teyit edildi.
- **Rol seçim ekranındaki buton metni düzeltildi** — "Operatörüm" ve "Bu Cihaz İnceleniyor" kartlarının ikisi de aynı generic "Bunu Seç" yazısını taşıyordu (kullanıcı geri bildirdi). Artık her kart kendi eylemini anlatan bir metin taşıyor: "Operatör Olarak Devam Et" / "Bu Cihazla Devam Et" (detaylar [hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)'da).
- **Küçük, kapsam-bilinçli iyileştirmeler** (bir "Product Manager" değerlendirmesiyle önerilip onaylanan 7 madde; hiçbiri yeni bağımlılık gerektirmedi):
  - **Rapor sürüm hatası düzeltildi** — `forensic_report.py`'deki `TOOL_VERSION` sabit `"1.0"` yazılmıştı, oysa gerçek uygulama sürümü `shared/version.py`'de ayrı yönetiliyordu (`0.1.0`) — rapor, üretildiği andaki gerçek sürümü değil sabit/yanlış bir değeri gösteriyordu. Artık `version.py`'den okunuyor (bkz. [hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)).
  - **Rapor dosyaları için sidecar `.sha256`** — `report.save()` artık `report.json.sha256` de yazıyor (dosyanın o anki içeriğinin hash'i). Raporun kendisinin kaydedildikten sonra değiştirilip değiştirilmediğini bağımsız olarak göstermenin ilk, hafif adımı.
  - **Bağımsız doğrulama CLI'si** — yeni `engines/ssh_engine/local_collector/verify_report.py`: `python verify_report.py <report.json>` çalıştırıldığında hem report.json'un sidecar hash'e karşı bütünlüğünü hem de imaj dosyasının rapordaki `image_hash`'e karşı GÜNCEL halini kontrol eder. GUI'ye hiç bağımlı değil, ikinci bir uzman/denetçi Chameleon'un kendi arayüzüne güvenmeden aynı sonuca ulaşabilir.
  - **Yerel disk alanı ön kontrolü** — `image_acquirer.acquire_disk_image`/`windows_acquirer.acquire_disk_image_windows`, imaj almaya başlamadan önce (`shutil.disk_usage`) yerel çıktı klasöründeki boş alanı hedef diskin boyutuyla karşılaştırıyor; yetersizse işlem hiç başlamadan durduruluyor (saatler süren bir aktarımın sonda "disk doldu"ya çarpmasını önler). Resume'da (`start_block > 0`) bu kontrol atlanır.
  - **Disk model/seri no kaydı** — Linux tarafında `lsblk`'e `MODEL,SERIAL` kolonları eklendi, imaj alma başlarken `lsblk -P` (key="value" formatı, MODEL alanındaki boşlukların kolonları bozmasını önlemek için) ile seçili diskin model/seri no'su ayrıca sorgulanıp rapora `source_description` olarak yazılıyor; Windows tarafında `Get-Disk`'in `FriendlyName`/`SerialNumber`'ı aynı amaçla kullanılıyor. Böylece rapor sadece `/dev/sdb` gibi bir yol değil, diskin kalıcı/benzersiz kimliğini de taşıyor (ISO/IEC 27037'nin istediği bir gereksinim).
  - **Son bağlanılan hedefler hafızası** — Host alanına `QCompleter` eklendi; başarılı bir bağlantıdan sonra host/port/kullanıcı adı (şifre KESİNLİKLE hariç) `shared/data/recent_ssh_hosts.json`'a (son 5, `.gitignore`'da) kaydediliyor. Listeden bir host seçilince port/kullanıcı adı da otomatik dolduruluyor.
  - **SSH bağlantı zaman aşımı artık koddan ayarlanabilir** — `SSHConnector`'a `connect_timeout` parametresi eklendi (varsayılan yine 10sn, davranış değişmedi). Yüksek gecikmeli (örn. Tor) bağlantılarda ileride ihtiyaç olursa GUI'ye ek bir "gelişmiş ayarlar" paneli eklemeden çağıran taraf bunu değiştirebilir; `image_acquirer.ensure_connection`'ın deneme bekleme süreleri (`wait_seconds`) zaten parametrikti, sadece hiç dışarıya açılmamıştı.
  - **Vaka Geçmişi CSV dışa aktarma önerisi ASKIYA ALINDI** — bu maddeyi uygularken, roadmap'in daha önce "Yapıldı" dediği launcher'daki **"Vaka Geçmişi" sidebar sayfasının kodda hiç bulunmadığı** ortaya çıktı (bkz. hemen altındaki not ve [hatalar_ve_sonuclar.md](hatalar_ve_sonuclar.md)). CSV export'un ekleneceği bir sayfa olmadığı için bu madde uygulanmadı.

**Not — doküman/kod tutarsızlığı bulundu:** Yukarıdaki "VPN bağlantı yöntemi + ... + Vaka Geçmişi eklendi" maddesi, launcher'da bir "Vaka Geçmişi" sidebar sayfası olduğunu söylüyor; bu artık DOĞRU DEĞİL — `launcher/chameleon_gui.py`'nin sidebar'ında böyle bir sekme yok (muhtemelen `customtkinter` → PySide6 tam geçişinde launcher'ın kabuğu sıfırdan kurulurken sessizce düşürüldü). Backend (`shared/data/case_history.json` + `forensic_report.read_history()`) hâlâ her rapor kaydında güncelleniyor ve sağlam — sadece onu gösteren UI sayfası eksikti. **Aşağıda "Yapıldı"ya taşındı** (yeniden eklendi).

- **"Sırada" listesinden 4 madde tamamlandı: Vaka Geçmişi sayfası, sıkıştırma, dosya modunda resume, gerçek portable yapı**
  - **"Vaka Geçmişi" sidebar sayfası yeniden eklendi** — `launcher/chameleon_gui.py`'ye yeni bir "history" sekmesi + `_show_case_history()`: `forensic_report.read_history()`'nin listesini vaka başına bir kart olarak gösterir (motor, hedef, inceleyen, yetkili kişi, tarih, durum renkli, "Raporu Aç" — sadece HTML dosyası gerçekten hâlâ diskteyse görünür). Yeni bir **"CSV Olarak Dışa Aktar"** butonu, tüm listeyi stdlib `csv` ile (Excel'de Türkçe karakterlerin doğru görünmesi için `utf-8-sig`) dışa aktarır. Bu sayfayı test ederken `_clear_content()`'te (TÜM sayfa geçişlerinin kullandığı ortak fonksiyon) bulunan bir yan hata da düzeltildi: `deleteLater()` asenkron olduğu için, art arda hızlı sayfa değişimlerinde eski sayfanın widget'ları bir süre daha "var" sayılıyordu (aynı sınıf, daha önce Bilgi Merkezi'nde bulunan bug) — `hide()` eklendi.
  - **Offline Acquisition'da isteğe bağlı gzip sıkıştırma eklendi** — "Hedef Disk ve İşlem Modu" kartına yeni bir `widgets.Checkbox` (tasarım sisteminde ilk kez eklenen checkbox bileşeni, `RadioButton` ile aynı çizim/odak deseni) ile "Sıkıştır (gzip)" seçeneği geldi, SADECE Offline modda aktif. İmaj birleştirilip HAM içeriğin SHA-256'sı (`image_hash`) hesaplandıktan SONRA sıkıştırılıyor ve ham dosya siliniyor — kritik nokta: rapordaki `image_hash`/`total_bytes` HER ZAMAN sıkıştırılmamış içeriğe ait kalıyor, sadece `output_path` artık `.gz` dosyasını gösteriyor (delil bütünlüğü hash'i, depolama formatından bağımsız). Sıkıştırılmış bir imaj için GUI'nin otomatik "İmaj Doğrula" adımı atlanıyor (kullanıcıya `verify_report.py` kullanması söyleniyor) — `verify_report.py`'ye de gzip farkındalığı eklendi (`.gz` uzantılı `output_path` görürse dosyayı açıp HAM içeriği hash'liyor). Yeni bir chain-of-custody olayı: `EVENT_IMAGE_COMPRESSED` (ham/sıkıştırılmış boyut farkını kaydeder).
  - **`file_acquirer.py`'de büyük dosyalarda resume eklendi** — tek dosya alma artık `image_acquirer.py` ile AYNI blok+doğrulama desenini kullanıyor (`dd bs=4M` ile parça parça, her parça `sha256sum` ile yazmadan önce doğrulanıyor) — eskiden dosyanın TAMAMI `cat` ile TEK SEFERDE hafızaya okunup TEK bir `sha256sum` ile doğrulanıyordu, birkaç GB'lık bir dosyada bağlantı kopması işlemi baştan başlatıyordu. Artık `ensure_connection()` (image_acquirer.py'den, değişmeden) ile aynı şekilde yeniden bağlanıp KALDIĞI BLOKTAN devam ediyor. `acquire_remote_file()`'ın imzası/dönüş değeri değişmedi, bu yüzden `acquire_remote_tree()` (klasör modu) hiç dokunulmadan aynen çalışmaya devam ediyor. **Kapsam notu**: bu, TEK BİR ÇALIŞTIRMA içindeki bağlantı kopmalarını (asıl şikayet edilen senaryo) otomatik olarak toparlıyor; disk imajlamadaki gibi programı tamamen kapatıp yeniden açtıktan sonra manifest'ten devam etme (tüm bir klasör ağacı için) bu kapsama dahil değil — o, çok daha büyük bir değişiklik (tree-genelinde kalıcı manifest) gerektirirdi.
  - **`Chameleon.exe` artık gerçekten portable** — derlenmiş modda beş farklı YAZILABİLİR yol (`chain_of_custody.LOG_DIR`, `image_acquirer.IMAGE_DIR`/`MANIFEST_DIR`, `ssh_connector.CHAMELEON_KNOWN_HOSTS`, `forensic_report.HISTORY_DIR`, `gui_v2.py`'deki `DEFAULT_IMAGE_PATH`/`entry_file_out` varsayılanı/operatör Tor anahtarı yolu/`_RECENT_HOSTS_FILE`) `__file__`'e göre hesaplanıyordu — PyInstaller'ın her çalıştırmada SİLİNEN geçici `_MEIPASS` klasörüne düşüyordu. Sadece roadmap'te adı geçen ikisi (LOG_DIR/HISTORY_DIR) değil, TOFU known_hosts ve operatör Tor anahtarı da (yazılamayan aynı hata sınıfı) bu oturumda keşfedilip aynı şekilde düzeltildi — aksi halde "gerçekten portable" iddiası eksik kalırdı. Düzeltme: her dosya kendi içinde `sys.frozen` kontrolü yapıp derlenmiş modda `sys.executable`'ın (exe'nin KENDİ, KALICI konumu) dizinini kullanıyor; kaynaktan çalışırken davranış hiç değişmedi (regresyonla doğrulandı). Salt-okunur, PyInstaller'ın paketlediği kaynaklar (ui_kit, ikonlar, fontlar, gömülü tor.exe/RamImagerCLI.exe) bilinçli olarak DOKUNULMADI -- onlar zaten `_MEIPASS`'ta olması gereken yerde.
  - Tüm dördü de mock SSH ile ve gerçek `sys.frozen`/`sys.executable` simülasyonuyla (hem frozen hem kaynak modda) ayrı ayrı test edildi; önceki tüm regresyon testleri (rol seçimi, son bağlantılar hafızası, disk boşluğu kontrolü) tekrar çalıştırılıp bozulma olmadığı teyit edildi.
- **Sahaya götürülecek ayrı, hedef-taraf-only bir `.exe` eklendi** — kullanıcı, operatörün kullandığı tam `Chameleon.exe`'nin (SSH/RAM motorları, Bilgi Merkezi, Vaka Geçmişi dahil TÜM operatör araçları) sahaya götürülen kopyasında sadece "Bu Cihaz İnceleniyor" sihirbazının olmasının hem daha sade/kafa karıştırmayan hem de daha hafif olacağını önerdi. Yeni `build_target_kit.spec` + ince bir giriş noktası (`launcher/target_kit_main.py`, `CHAMELEON_TARGET_ONLY` ortam değişkenini `chameleon_gui` import edilmeden önce ayarlar) ile `dist/ChameleonHedefKiti.exe` üretiliyor — AYNI `chameleon_gui.py` kullanılıyor (kod tekrarı yok), sadece `ChameleonWindow.__init__` bu değişkeni görünce rol seçim ekranını atlayıp doğrudan hedef sihirbazını açıyor ("Geri" butonu da gizleniyor, dönülecek bir rol seçim ekranı olmadığı için). `engines/ssh_engine/local_collector/*` (dolayısıyla `paramiko`) ve `engines/ram_engine/*` (`RamImagerCLI.exe` dahil) bu derlemeye HİÇ paketlenmiyor. **Dürüst boyut notu**: beklenenin aksine tasarruf küçük çıktı (~57.7 MB -> ~56.7 MB, ~%2) — PySide6/Qt çalışma zamanı ve gömülü `tor.exe` (10 MB, HER İKİ tarafta da gerekli) toplam boyutu zaten domine ediyor, `paramiko`/`RamImagerCLI.exe` görece küçük. Asıl kazanç dosya boyutu değil, sahadaki teknik bilgisi olmayan kişinin hiçbir zaman ilgisiz bir SSH/RAM arac seti görmemesi. Gerçek derlenmiş `.exe` başlatılıp (tıklama yapılmadan, Windows UI Automation ile salt-okunur metin/buton okuması) doğrudan hedef sihirbazına gittiği ve "Geri" butonunun gerçekten yok olduğu doğrulandı; aynı yöntemle tam `Chameleon.exe`'nin rol seçim ekranını hâlâ gösterdiği (regresyon) de teyit edildi.

## Sırada

1. **Taşınabilir toplama kiti — kalan işler:**
   - **Operatörün gerçek `.onion` adresine bağlanması** — hedef taraf (hidden service kurulumu) artık gerçek Tor ile çalışıyor ve doğrulandı (bkz. "Yapıldı"). Kalan: operatörün `tor_client.py` ile o adrese SOCKS5 üzerinden bağlanıp SSH kurabildiğinin doğrulanması — döngünün ikinci yarısı, muhtemelen iki ayrı makine/ağ gerektiriyor. Kullanıcıyla birlikte yapılacak.
   - ~~Tek, dağıtılabilir "kit" paketi~~ **tamamlandı** — `dist/ChameleonHedefKiti.exe` (bkz. "Yapıldı").
   - **Digital imza / paket imzalama** — v2.0'a bırakıldı, düşük öncelik (bkz. "Daha sonra" bölümü).
   - Şu an SADECE Windows hedef için (gömülü Tor binary'si Windows x86_64). Linux hedef desteği ayrı bir adım.
2. **Linux hedefte RAM alma (askıda)** — LiME gibi bir çözüm gerekiyor ama hedefin çalışan kernel'ine özel derleme istiyor (Windows'taki gibi tek bir hazır dosya yetmiyor); kapsam daraltılmadan (örn. "sadece şu kernel sürümleri" gibi) ele alınması gerçekçi değil.
3. **Full RAM modunun gerçek makinede test edilmesi** — `ram_gui.py`'deki full mod kodu yazıldı (ShellExecute+runas ile yükseltme, `.img.log` tail'leme) ama Yönetici + Secure Boot/test-signing gerektirdiği için ben test edemedim; kullanıcı kendi makinesinde denemeli.

## Daha sonra, öncelik sırası netleşmedi

- Taşınabilir kit için dijital imza / paket imzalama (isteğe bağlı, düşük öncelik — v2.0)
- Gerçek EWF/E01 ve AFF4 desteği
- Büyük imajları `.001`/`.002` gibi parçalara bölme
- Aynı anda birden fazla istemciden imaj alma
- `ram_engine`'in sürücüsünü gerçek bir sertifikayla imzalamak (şu an test-signing + reboot gerekiyor)
- Ağ hızına göre değişen chunk boyutu
- Otomatik testler (pytest) — `ssh_engine`'de mock SSH deseni zaten var, oradan genişletilebilir
- Çoklu dil desteği — launcher'da başladı, motorların kendi arayüzüne henüz yayılmadı
